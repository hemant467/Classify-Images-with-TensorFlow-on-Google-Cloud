# Classify-Images-with-TensorFlow-on-Google-Cloud
End-to-end machine learning workflow on Google Cloud Vertex AI. Includes training a CNN for image classification with a custom Python script, running training via Vertex AI Custom Jobs, deploying the model to an online prediction endpoint, and sending prediction requests to validate deployment.
